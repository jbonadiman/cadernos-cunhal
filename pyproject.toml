[project]
name = "cadernos-cunhal-web"
# Bump this on every change that touches extract/ or its tests.
# See AGENTS.md.
version = "0.1.0"
description = "Extraction pipeline for the Cadernos Álvaro Cunhal facsimile viewer"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
